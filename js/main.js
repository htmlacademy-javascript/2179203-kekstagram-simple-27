let commentLength = function (test_string, max_length) {
     if(test_string.length > max_length){
           return true;
     } else {
        return false;
     }
}
commentLength('Функция для проверки максимальной длины строки',40);
